document.addEventListener("DOMContentLoaded",()=>{const form=document.querySelector("#post-ad-form"),status=document.querySelector("#post-status");form.addEventListener("submit",e=>{e.preventDefault();const title=new FormData(form).get("title");status.textContent='Thanks — "'+title+'" was captured by this starter interface. The production backend will place it into moderation before publication.';form.reset()})});